namespace Common.Settings;


// we created a section with this name in appsettings.json in the project who is using it.
public class ServiceSettings 
{
    public string? ServiceName { get; init; }
}
